{
  "scripts": {
    "build": "babel --presets es2015 bot -d build",
    "start": "node build/bot.js",
    "full": "yarn build && yarn start",
    "flo": "yarn build && node build/floBot.js"
  },
  "dependencies": {
    "isomorphic-fetch": "^2.2.1",
    "moment": "^2.24.0",
    "moment-precise-range-plugin": "^1.3.0",
    "tmi.js": "^1.4.2",
    "websocket": "^1.0.29"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.3",
    "babel-preset-es2015": "^6.24.1"
  }
}
